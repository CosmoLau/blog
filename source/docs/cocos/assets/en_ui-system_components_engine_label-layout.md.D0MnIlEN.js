import{_ as e,c as o,o as t,a4 as n}from"./chunks/framework.uQk9_EO2.js";const a="/docs/cocos/assets/label_in_boundingbox.Drq7dxDj.png",i="/docs/cocos/assets/horizontal-vertical-align.BwRMb6tt.png",l="/docs/cocos/assets/font_equal_line_height.ERbk5V3k.png",s="/docs/cocos/assets/font_smaller.lVKYb77H.png",r="/docs/cocos/assets/font_bigger.DJVFhBvp.png",h="/docs/cocos/assets/clamp.DhJUCMO2.png",d="/docs/cocos/assets/shrink.p8N5TSIF.png",c="/docs/cocos/assets/resize-height.kDbkSwbs.png",p="/docs/cocos/assets/clamp_wrap.4mSSKwsh.png",b="/docs/cocos/assets/clamp_wrap1.Coy6lWsu.png",g="/docs/cocos/assets/clamp_wrap2.LffFzug4.png",u="/docs/cocos/assets/shrink_wrap.Bfzkoo9e.png",m="/docs/cocos/assets/anchor1.CWwryiJx.png",f="/docs/cocos/assets/anchor2.0T8jpujO.png",w="/docs/cocos/assets/widget.DpVcdfFU.png",A=JSON.parse('{"title":"Label Layout","description":"","frontmatter":{},"headers":[],"relativePath":"en/ui-system/components/engine/label-layout.md","filePath":"en/ui-system/components/engine/label-layout.md","lastUpdated":1712305443000}'),y={name:"en/ui-system/components/engine/label-layout.md"},_=n('<h1 id="label-layout" tabindex="-1">Label Layout <a class="header-anchor" href="#label-layout" aria-label="Permalink to &quot;Label Layout&quot;">​</a></h1><p>The <strong>Label</strong> component is one of the key renderable components. You need to learn about how to set up label layout in order to have a perfect display when the UI system adapts to various resolutions and sets up alignment.</p><h2 id="alignment-of-labels-in-the-bounding-box" tabindex="-1">Alignment of labels in the bounding box <a class="header-anchor" href="#alignment-of-labels-in-the-bounding-box" aria-label="Permalink to &quot;Alignment of labels in the bounding box&quot;">​</a></h2><p>Like other renderer components, the layout of the Label component is also based on the size information (<code>contentSize</code>) possessed by the <a href="./../editor/ui-transform">UITransform</a> component, that is, the range specified by the Bounding Box.</p><p><img src="'+a+'" alt="label in bounding box"></p><p>What is shown in the picture above is the display effect of labels rendered by Label in the blue bounding box. The following properties in Label determines the position of labels in the bounding box:</p><ul><li><p><code>Horizontal Align</code>: the horizontal alignment of labels in the bounding box, which can be chosen from 3 positions: Left, Right, Center.</p></li><li><p><code>Vertical Align</code>: the vertical alignment of labels in the bounding box, which can be chosen from 3 positions: Top, Bottom, Center.</p><p><img src="'+i+'" alt="horizontal-vertical-align"></p></li></ul><p>In the figure above, the horizontal alignment position is set to <code>Right</code>, and the vertical alignment position is set to <code>Bottom</code>. It can be seen that the label appears at the bottom of the Bounding Box and is aligned to the right. The developer can modify the two properties above to make other combinations, labels will appear on the corresponding position of the blue bounding box according to the settings.</p><h2 id="label-size-and-line-height" tabindex="-1">Label Size and Line Height <a class="header-anchor" href="#label-size-and-line-height" aria-label="Permalink to &quot;Label Size and Line Height&quot;">​</a></h2><p><code>Font Size</code> determines the display size of labels. Its unit is Point (it can also be called &#39;pound&#39;), which is the size unit for fonts commonly used in most image editing softwares and font editing softwares. For dynamic fonts, <code>Font Size</code> can be zoomed in losslessly. But the display of bitmap fonts will be more and more vague when the set value of <code>Font Size</code> exceeds the font size stipulated by the font.</p><p><code>Line Height</code> determines the height occupied by each line when multiple lines of labels display, the unit of which is also Point. The displaying of multiple lines of labels can be carried out by using two methods:</p><ul><li>When inputting labels in the <code>String</code> property, manually input <code>\\r</code> or <code>\\n</code></li><li>Open the <code>Enable Wrap Text</code> property, which will be introduced in detail later</li></ul><p><strong>Relation of label size and line height</strong>:</p><ul><li><p>If the values of <code>Font Size</code> and <code>Line Height</code> are the same, labels will occupy the height of the most part of one line.</p><p><img src="'+l+'" alt="font_equal_line_height"></p></li><li><p>If the value of <code>Font Size</code> is less than that of <code>Line Height</code>, space between multiple lines of labels will be enlarged</p><p><img src="'+s+'" alt="font_smaller"></p></li><li><p>If the value of <code>Font Size</code> is larger than that of <code>Line Height</code>, space between multiple lines of labels will be narrowed between multiple lines of labels. Overlapping of labels may appear.</p><p><img src="'+r+'" alt="font_bigger"></p></li></ul><h2 id="overflow" tabindex="-1">Overflow <a class="header-anchor" href="#overflow" aria-label="Permalink to &quot;Overflow&quot;">​</a></h2><p>The <strong>Overflow</strong> property determines the array of labels in the bounding box when the content of labels is increased. There are four modes: <strong>NONE</strong>, <strong>CLAMP</strong>, <strong>SHRINK</strong>, and <strong>RESIZE_HEIGHT</strong>. Only in the latter three modes can the size of the bounding box be adjusted through the <strong>Rectangle Transform Tool</strong> (or click the keyboard button <strong>T</strong>) in the upper left corner of the editor or modifying the <strong>Size</strong> in the <strong>Inspector</strong> panel or add the <strong>Widget</strong> component.</p><p><strong>NONE</strong> mode will automatically fix the size of the bounding box according to the text size, line height, etc.</p><h3 id="clamp" tabindex="-1">Clamp <a class="header-anchor" href="#clamp" aria-label="Permalink to &quot;Clamp&quot;">​</a></h3><p><img src="'+h+'" alt="clamp"></p><p>When in Clamp mode, labels will firstly be rendered according to the requirements of alignment type and size, but the parts exceeding the bounding box will be concealed (clamped).</p><h3 id="auto-shrink" tabindex="-1">Auto Shrink <a class="header-anchor" href="#auto-shrink" aria-label="Permalink to &quot;Auto Shrink&quot;">​</a></h3><p><img src="'+d+'" alt="shrink"></p><p>When in auto shrink mode, if labels exceed the bounding box when being rendered according to the original size, the size of the labels will be automatically shrink to display all the labels.</p><p><strong>Attention!</strong> Auto shrink mode will not zoom in on labels to adapt to bounding box.</p><h3 id="resize-height" tabindex="-1">Resize Height <a class="header-anchor" href="#resize-height" aria-label="Permalink to &quot;Resize Height&quot;">​</a></h3><p><img src="'+c+'" alt="resize-height"></p><p>Resize height mode will make sure the bounding box of the labels fits the height of the labels, no matter the quantity of the labels&#39; lines. This mode is suitable for displaying paragraphs of labels that don&#39;t have the same amount of content. An infinite amount of label content can be displayed in the arbitrary UI field when using this mode together with the <a href="./../components/scrollview">ScrollView</a> component.</p><h2 id="enable-wrap-text" tabindex="-1">Enable Wrap Text <a class="header-anchor" href="#enable-wrap-text" aria-label="Permalink to &quot;Enable Wrap Text&quot;">​</a></h2><p>The <code>Enable Wrap Text</code> property of the Label component can switch the auto wrap switch of labels. When <code>Enable Wrap Text</code> is opened, labels will automatically wrap acccording to the width of the bounding box without manual input of <code>\\r</code> or <code>\\n</code> when inputting labels.</p><blockquote><p><strong>Note</strong>: the <code>Enable Wrap Text</code> property is only available in the <strong>CLAMP</strong> and <strong>SHRINK</strong> modes of the label layout mode. In <strong>RESIZE_HEIGHT</strong> mode, the <code>Enable Wrap Text</code> property is compulsorily opened.</p></blockquote><h3 id="auto-wrap-in-clamp-mode" tabindex="-1">Auto Wrap in Clamp Mode <a class="header-anchor" href="#auto-wrap-in-clamp-mode" aria-label="Permalink to &quot;Auto Wrap in Clamp Mode&quot;">​</a></h3><p>When clamp mode opens auto wrap, labels will be wrapped in the allowable scale of the bounding box in priority. Only when not all the words can be displayed after being wrapped will clamp mode function.</p><p><img src="'+p+'" alt="clamp_wrap"></p><p>The following two pictures are taken when <code>Clamp</code> + <code>Enable Wrap Text</code> are opened, the difference of which is the different width of the bounding boxes of the labels;</p><p><img src="'+b+'" alt="clamp_wrap 1"></p><p><img src="'+g+'" alt="clamp_wrap 2"></p><p>When the width of the bounding box is changing from the left picture to the right picture, labels will be continuously adjusted and wrapped. In the end, the clamped display will appear due to the insufficient height of the bounding box.</p><h3 id="auto-wrap-in-auto-shrink-mode" tabindex="-1">Auto Wrap in Auto shrink mode <a class="header-anchor" href="#auto-wrap-in-auto-shrink-mode" aria-label="Permalink to &quot;Auto Wrap in Auto shrink mode&quot;">​</a></h3><p>Similar to clamp mode, labels will be wrapped in priority when labels exceed the width of the bounding box in auto shrink mode. Only when the width and length of the bounding box are exceeded will the labels be automatically shrink to adapt to the bounding box.</p><p><img src="'+u+'" alt="shrink_wrap"></p><h3 id="enable-wrap-text-of-chinese" tabindex="-1">Enable Wrap Text of Chinese <a class="header-anchor" href="#enable-wrap-text-of-chinese" aria-label="Permalink to &quot;Enable Wrap Text of Chinese&quot;">​</a></h3><p>The auto wrap behavior of Chinese is different from that of English. English is wrapped by the unit of words. Blank space is the smallest unit of wrap adjustment. Chinese is wrapped by the unit of characters. Every character can be wrapped alone.</p><h2 id="anchor-point-of-label-node" tabindex="-1">Anchor point of label node <a class="header-anchor" href="#anchor-point-of-label-node" aria-label="Permalink to &quot;Anchor point of label node&quot;">​</a></h2><p>The anchor point of a label node and the alignment mode of labels in the bounding box are two concepts that need to be differentiated. In a layout type that needs labels to enlarge the bounding box (e.g., <code>Resize Height</code>), only correct setting up of anchor points can make the bounding box be extended to the expected direction.</p><p>For example, if you want the bounding box to extend downward, you need to set the <code>y</code> property of <code>Anchor</code> as <code>1</code>. As shown below:</p><p><img src="'+m+'" alt="anchor1"></p><p><img src="'+f+'" alt="anchor2"></p><h2 id="widget" tabindex="-1">Widget <a class="header-anchor" href="#widget" aria-label="Permalink to &quot;Widget&quot;">​</a></h2><p>Add a <strong>Widget</strong> component to the node with the Label component, then you can make various layouts for label nodes relative to their parent nodes.</p><p><img src="'+w+'" alt="widget"></p><p>In the above picture, two Label child nodes are added to the background node. After respectively adding the Widget component to them, set the <code>Right</code> property of the Widget for the labels on the left side as <code>50%</code>, and set the <code>Left</code> property of the Widget for the labels on the right side as <code>60%</code>, then the multi-column layout of labels in the picture above can be realized.</p><p>And by setting margins on the Widget, plus the layout type of labels, a flexible and beautiful layout for labels can be easily realized without concrete and minor adjustments to the bounding box.</p><h2 id="reference-for-checking-components" tabindex="-1">Reference for checking components <a class="header-anchor" href="#reference-for-checking-components" aria-label="Permalink to &quot;Reference for checking components&quot;">​</a></h2><p>For properties of the Label component, you can also check <a href="./../editor/label">Label Component</a> document.</p>',54),x=[_];function k(z,T,q,v,W,L){return t(),o("div",null,x)}const E=e(y,[["render",k]]);export{A as __pageData,E as default};
