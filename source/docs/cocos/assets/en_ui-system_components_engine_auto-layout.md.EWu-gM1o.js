import{_ as o,c as e,o as t,a4 as i}from"./chunks/framework.uQk9_EO2.js";const a="/docs/cocos/assets/layout-node.K6Y2paW4.png",n="/docs/cocos/assets/horizontal-no-align.D_SxdZf-.png",r="/docs/cocos/assets/horizontal-resizemode.BCFFqDmu.png",c="/docs/cocos/assets/grid-layout.jY4GWfap.png",_=JSON.parse('{"title":"Auto Layout Container","description":"","frontmatter":{},"headers":[],"relativePath":"en/ui-system/components/engine/auto-layout.md","filePath":"en/ui-system/components/engine/auto-layout.md","lastUpdated":1712305443000}'),d={name:"en/ui-system/components/engine/auto-layout.md"},l=i('<h1 id="auto-layout-container" tabindex="-1">Auto Layout Container <a class="header-anchor" href="#auto-layout-container" aria-label="Permalink to &quot;Auto Layout Container&quot;">​</a></h1><p>The <strong>Layout</strong> component can be mounted to any <strong>Node</strong>, making the node into a container with the auto layout function. The so-called auto layout container can automatically array the child nodes according to certain rules and adjust the container type nodes of its own size according to the sum total of bounding boxes of the node content.</p><p>For the next layout types, the node structure is as follows:</p><p><img src="'+a+'" alt="layout-node"></p><h2 id="layout-type" tabindex="-1">Layout Type <a class="header-anchor" href="#layout-type" aria-label="Permalink to &quot;Layout Type&quot;">​</a></h2><p>Auto layout components have several basic layout types. These can be set up by the <code>Layout Type</code> property, which includes the following types.</p><h3 id="horizontal-layout" tabindex="-1">Horizontal Layout <a class="header-anchor" href="#horizontal-layout" aria-label="Permalink to &quot;Horizontal Layout&quot;">​</a></h3><p><img src="'+n+'" alt="horizontal-no-align"></p><p>When Layout <code>Type</code> is set to <code>Horizontal</code>, all child nodes will be automatically aligned horizontally and the component will modify the position or height of the node on the y-axis by default. If the child node needs to be placed outside the height range of the Layout node&#39;s bounding box, you can uncheck <code>AutoAlignment</code> (as shown above).</p><p>The situation that the content exceeds the container easily under horizontal sorting, the following measures can be taken as needed.</p><ul><li><p>If the container is to adapt to the size of the content, you can set the <code>ResizeMode</code> to <code>Container</code>, which will set the width of the Layout node based on the sum of the widths of the child nodes (<code>Width</code>) (left in the figure below).</p></li><li><p>If the content object is always to remain inside the container, you can set <code>ResizeMode</code> to <code>Children</code>, which will limit the size of the content object to the container (right in the figure below).</p></li><li><p>If you want the child nodes to be aligned upwards on the y-axis, you can add a widget component to the child node and turn on the <code>Top</code> or <code>Bottom</code> alignment mode.</p></li></ul><p><img src="'+r+'" alt="horizontal-resizemode"></p><h4 id="horizontal-direction" tabindex="-1">Horizontal Direction <a class="header-anchor" href="#horizontal-direction" aria-label="Permalink to &quot;Horizontal Direction&quot;">​</a></h4><p>In the horizontal layout, you can set the horizontal orientation with <code>HorizontalDirection</code>. There are two types of orientation: <code>LEFT_TO_RIGHT</code> and <code>RIGHT_TO_LEFT</code>. The former will arrange the nodes from left to right according to their display order in <strong>Hierarchy</strong>. The latter will arrange the nodes from right to left according to their display order.</p><h3 id="vertical-layout" tabindex="-1">Vertical Layout <a class="header-anchor" href="#vertical-layout" aria-label="Permalink to &quot;Vertical Layout&quot;">​</a></h3><p>The layout and orientation of vertical layout is almost the same as <strong>horizontal layout</strong>, only the orientation is different.</p><h3 id="grid-layout" tabindex="-1">Grid Layout <a class="header-anchor" href="#grid-layout" aria-label="Permalink to &quot;Grid Layout&quot;">​</a></h3><p>Layout <code>Type</code> set to <code>Grid</code> will start the grid layout. The grid layout will determine the starting point of the layout based on the combination of <code>HorizontalDirection</code> and <code>VerticalDirection</code> within a fixed container size, and the layout direction based on the <code>StartAxis</code> property.</p><h4 id="grid-direction" tabindex="-1">Grid Direction <a class="header-anchor" href="#grid-direction" aria-label="Permalink to &quot;Grid Direction&quot;">​</a></h4><p>Layout arranges the child nodes in the order in which they are displayed in the <strong>Hierarchy</strong>, plus the start point and the alignment direction set by the <code>StartAxis</code> property.</p><ul><li>Start axis <ul><li>Set to either <code>HORIZONTAL</code> or <code>VERTICAL</code> orientation. The former will be aligned horizontally, the latter vertically.</li></ul></li><li>Start point <ul><li>The start point is created by combining <code>HorizontalDirection</code> and <code>VerticalDirection</code>.</li><li>Suppose <code>HorizontalDirection</code> is <code>LEFT_TO_RIGHT</code> and VerticalDirection is <code>TOP_TO_BOTTOM</code>, then the start point is <strong>top left</strong>.</li><li>Suppose <code>HorizontalDirection</code> is <code>RIGHT_TO_LEFT</code> and VerticalDirection is <code>BOTTOM_TO_TOP</code>, then the start point is <strong>bottom right</strong>.</li></ul></li></ul><p>Two examples are given in conjunction with alignment directions:</p><ul><li><p>If the <code>HorizontalDirection</code> is set to <code>LEFT_TO_RIGHT</code>, <code>VerticalDirection</code> is <code>TOP_TO_BOTTOM</code> and <code>StartAxis</code> is <code>HORIZONTAL</code>. This tells the component to be sorted horizontally starting from the <strong>top left</strong> of the container (below left).</p></li><li><p>If the currently set <code>HorizontalDirection</code> is <code>RIGHT_TO_LEFT</code>, VerticalDirection is <code>BOTTOM_TO_TOP</code> and <code>StartAxis</code> is <code>VERTICAL</code>, it is telling the component to be sorted vertically starting from the <strong>BOTTOM RIGHT</strong> of the container (right in the figure below).</p><p><img src="'+c+'" alt="grid-layout"></p></li></ul><p>Grid sorting may also cause the content to exceed the container, which can also be solved by using the <code>Children</code> and <code>Container</code> modes of <code>ResizeMode</code> mentioned in <code>Horizontal Layout</code>.</p><p>For more information, please refer to the <a href="./../editor/layout">Layout component</a> documentation.</p>',25),s=[l];function h(u,p,y,g,m,f){return t(),e("div",null,s)}const b=o(d,[["render",h]]);export{_ as __pageData,b as default};
