import{_ as e,c as a,o as s,a4 as i}from"./chunks/framework.uQk9_EO2.js";const t="/docs/cocos/assets/folder3.CTZz3JUI.png",o="/docs/cocos/assets/folder4.v8JotJ5T.png",n="/docs/cocos/assets/project.Bu6nqPtf.png",l="/docs/cocos/assets/code1.DYtZ62gS.png",c="/docs/cocos/assets/code2.Bbt4kbWW.png",C=JSON.parse('{"title":"Introduction to CMake Usage","description":"","frontmatter":{},"headers":[],"relativePath":"en/advanced-topics/cmake-learning.md","filePath":"en/advanced-topics/cmake-learning.md","lastUpdated":1712305443000}'),r={name:"en/advanced-topics/cmake-learning.md"},d=i('<h1 id="introduction-to-cmake-usage" tabindex="-1">Introduction to CMake Usage <a class="header-anchor" href="#introduction-to-cmake-usage" aria-label="Permalink to &quot;Introduction to CMake Usage&quot;">​</a></h1><p>CMake is a very powerful build tool that can greatly simplify the software compilation process and improve development efficiency. Cocos Creator uses CMake on various native platforms. Here are some quick advantages of CMake:</p><ul><li>CMake uses the CMakeLists.txt file to describe the entire project&#39;s build process, rather than using script files like other build tools.</li><li>CMake is cross-platform and can run on operating systems such as Windows, Linux, and macOS.</li><li>CMake can automatically generate Makefiles, Visual Studio, and other IDE project files, simplifying the software compilation process.</li><li>CMake can easily manage dependency libraries, organize code into modules, etc.</li><li>CMake supports multiple programming languages, including C, C++, Fortran, Java, Python, etc.</li></ul><p>Although CMake is a very powerful build tool, it also has some drawbacks, such as its complex syntax and the need for a certain learning cost.</p><p>Developers can learn the syntax of CMake and add their modules to perform specific tasks during the build process. For example, they can define their own preprocessor macros or compiler options to execute custom operations during the build. In addition, they can write scripts to modify project files for different configurations on different platforms, and review <a href="./../editor/publish/native-options#secondary-development">Secondary Development</a>.</p><p>If you want to learn CMake in-depth, you can read other chapters of this tutorial to learn how to install CMake, create CMakeLists.txt files, specify compilers, add source files, add dependency libraries, and build projects, etc. Due to length limitations, this article cannot provide detailed information on how to use FindPackage, set build types, install and test, etc. To master it better, developers need to consult other documents and engage in more practice.</p><h2 id="installing-cmake" tabindex="-1">Installing CMake <a class="header-anchor" href="#installing-cmake" aria-label="Permalink to &quot;Installing CMake&quot;">​</a></h2><p>For the convenience of developers, Cocos Creator integrates the CMake program internally, which will be used in the build process. Therefore, in general, developers do not need to manually install CMake.</p><p>If developers want to use the CMake on their device, they can edit the relevant configuration to do so.</p><p>If developers want to use CMake in the command line, they can go to <a href="https://cmake.org/download/" target="_blank" rel="noreferrer">the official website</a> to download it. On the Mac platform, they can also use Homebrew to install it by executing the following command:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">brew</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> cmake</span></span></code></pre></div><h2 id="getting-started-with-cmake" tabindex="-1"><strong>Getting Started with CMake</strong> <a class="header-anchor" href="#getting-started-with-cmake" aria-label="Permalink to &quot;**Getting Started with CMake**&quot;">​</a></h2><p>The <code>CMakeLists.txt</code> file is the core file of CMake, which describes the entire project&#39;s build process. Using this file, you can easily manage the project&#39;s building and compilation processes. It contains a series of commands and variables used to specify project names, version numbers, source files, dependency libraries, compiler specifications, compilation options, and other parameters.</p><p>Here is a simple example of a CMake hello-world project:</p><p>First, create a file named <code>CMakeLists.txt</code>. In this file, add the following content:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span># CMake Version</span></span>\n<span class="line"><span>cmake_minimum_required(VERSION 3.10)</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span># Set project name and main language</span></span>\n<span class="line"><span>project(helloworld CXX)</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>add_executable(helloworld main.cpp)</span></span></code></pre></div><p>Then, create a file named <code>main.cpp</code> in the root directory of the project and add the following content:：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>#include &lt;iostream&gt;</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>int main() {</span></span>\n<span class="line"><span>    std::cout &lt;&lt; &quot;Hello, world!&quot; &lt;&lt; std::endl;</span></span>\n<span class="line"><span>    return 0;</span></span>\n<span class="line"><span>}</span></span></code></pre></div><p>Finally, create a directory named <code>build</code> in the root directory of the project and execute the following command inside it:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Generate the default project files in the build directory. If Visual Studio is installed, it defaults to a Visual Studio project; on Mac, it defaults to a Makefile project. The project file type can be set by specifying -G, for example, -GXcode.</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -B</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -S</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> .</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>\n<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Generate executable target</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cmake</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --build</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> build</span></span></code></pre></div><p>After executing these commands, an executable file named <code>helloworld</code> will be generated in the <code>build</code> directory. Running the file will output &quot;Hello, world!&quot;.</p><p>Two commands used here are <code>project</code> and <code>add_executable</code>.</p><p><code>project</code> is a command in CMake used to specify project name, version number, programming language, and other information. Its syntax is as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>project(project_name [version] [LANGUAGES languages...])</span></span></code></pre></div><p>Here, <code>project_name</code> specifies the project name, <code>version</code> specifies the version number of the project, and <code>languages</code> specifies the programming languages used in the project. If <code>version</code> or <code>languages</code> are not specified, they can be omitted. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>project(MyProject)</span></span></code></pre></div><p>This command sets the project name as <code>MyProject</code>, without specifying the version number and programming language.</p><p><code>add_executable</code> is used to add build rules for executable files, and its syntax is as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>add_executable(executable_name [source1] [source2] ...)</span></span></code></pre></div><p>Where <code>executable_name</code> is used to specify the name of the executable file, and <code>source1</code>, <code>source2</code>, and other parameters are used to specify the names of source files. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>add_executable(MyProject main.cpp)</span></span></code></pre></div><p>This command will set the executable file name to <code>MyProject</code> and add the <code>main.cpp</code> file as a source file to the project.</p><h3 id="other-commonly-used-cmake-commands" tabindex="-1">Other commonly used CMake commands <a class="header-anchor" href="#other-commonly-used-cmake-commands" aria-label="Permalink to &quot;Other commonly used CMake commands&quot;">​</a></h3><h3 id="message" tabindex="-1"><strong><code>message</code></strong> <a class="header-anchor" href="#message" aria-label="Permalink to &quot;**`message`**&quot;">​</a></h3><p>The <code>message()</code> command is used to display messages in the CMake console during the build process. It can be used for debugging, testing, and other purposes. Its syntax is as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>message([&lt;mode&gt;] &quot;message text&quot; ...)</span></span></code></pre></div><p>The <code>mode</code> parameter specifies the message type, which can be <code>STATUS</code>, <code>WARNING</code>, <code>AUTHOR_WARNING</code>, <code>SEND_ERROR</code>, <code>FATAL_ERROR</code>, or <code>DEPRECATION</code>. The <code>message text</code> parameter specifies the message to be displayed. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>message(STATUS &quot;This is a status message.&quot;)</span></span>\n<span class="line"><span>message(WARNING &quot;This is a warning message.&quot;)</span></span>\n<span class="line"><span>message(AUTHOR_WARNING &quot;This is an author warning message.&quot;)</span></span>\n<span class="line"><span>message(SEND_ERROR &quot;This is a send error message.&quot;)</span></span>\n<span class="line"><span>message(FATAL_ERROR &quot;This is a fatal error message.&quot;)</span></span>\n<span class="line"><span>message(DEPRECATION &quot;This is a deprecation message.&quot;)</span></span></code></pre></div><p>The message type determines the text color and icon displayed in the CMake console. For example, <code>STATUS</code> messages are displayed in green, while <code>WARNING</code> messages are displayed in yellow with a warning icon. The <code>FATAL_ERROR</code> message will stop the build process and display an error message in the console.</p><h3 id="set" tabindex="-1"><code>set</code> <a class="header-anchor" href="#set" aria-label="Permalink to &quot;`set`&quot;">​</a></h3><p>The <code>set()</code> command is mainly used to create or modify variables. The command accepts at least two arguments: the variable name and its value. For example, you can use <code>set(SRC_FILES main.cpp)</code> to set the value of the variable <code>SRC_FILES</code> to <code>main.cpp</code>. If you want to set multiple values (such as a list) for a variable, you can add more arguments to the command, like <code>set(SRC_FILES main.cpp util.cpp)</code>. To read the value of a variable, you can use the <code>${}</code> syntax, like <code>message(${SRC_FILES})</code>.</p><p>You can use the <code>set</code> command to add elements to a list variable. Specifically, you can use the command <code>set(SRC_FILES ${SRC_FILES} util.cpp)</code> to add <code>util.cpp</code> to the end of the <code>SRC_FILES</code> list. <code>${SRC_FILES}</code> represents the current value of the <code>SRC_FILES</code> variable. This command can also use other <code>set</code> command options, such as <code>CACHE</code> and <code>APPEND</code>.</p><h3 id="list" tabindex="-1"><code>list</code> <a class="header-anchor" href="#list" aria-label="Permalink to &quot;`list`&quot;">​</a></h3><p>The <code>list()</code> command is used for handling list-type variables. It can accept multiple subcommands, such as <code>APPEND</code> (adds an element to the end of the list), <code>INSERT</code> (inserts an element at a specified position), <code>REMOVE_ITEM</code> (removes a specified element), and so on. For example, the <code>list(APPEND SRC_FILES util.cpp)</code> command adds <code>util.cpp</code> to the end of the <code>SRC_FILES</code> list.</p><h3 id="add-library" tabindex="-1"><code>add_library</code> <a class="header-anchor" href="#add-library" aria-label="Permalink to &quot;`add_library`&quot;">​</a></h3><p>The <code>add_library</code> command is used to define a library target. It requires at least two arguments: the name of the library and the source file. If you provide only one source file, CMake will create a library built from that file. For example, <code>add_library(MyLib main.cpp)</code>. If you have multiple source files, you can put them all in the <code>add_library()</code> command, like <code>add_library(MyLib main.cpp util.cpp)</code>.</p><p>CMake supports creating static libraries and dynamic libraries. By default, the <code>add_library()</code> command creates a static library. If you want to create a dynamic library, you need to add the <code>SHARED</code> parameter to the command, like <code>add_library(MyLib SHARED main.cpp)</code>.</p><p>If you want to create both static and dynamic libraries, you can list them both, like <code>add_library(MyLibStatic STATIC main.cpp)</code> and <code>add_library(MyLibShared SHARED main.cpp)</code>.</p><p>A static library is a library linked to the executable file at compile time, while a dynamic library is a library loaded at runtime. Static libraries usually only contain the code needed by the executable file, so they are smaller. Dynamic libraries usually contain more code and data, as they need to be executed at runtime. The advantage of dynamic libraries is that they can be updated without recompiling the executable file. They can also be shared among multiple executable files, saving disk space.</p><p>When using the <code>add_library()</code> command, you can specify the name and type (static or dynamic) of the library, as well as the source files and header files to include. For example, the <code>add_library(MyLib STATIC main.cpp)</code> command adds the <code>main.cpp</code> source file to the static library named <code>MyLib</code>.</p><h3 id="find-library" tabindex="-1"><code>find_library</code> <a class="header-anchor" href="#find-library" aria-label="Permalink to &quot;`find_library`&quot;">​</a></h3><p>The command is used to locate library files on the system. You need to provide a variable name (to store the path of the found library) and the name of the library. For example, <code>find_library(MY_LIB NAMES MyLib)</code>. In this example, CMake will search for the library named <code>MyLib</code> in the system&#39;s library paths. If found, the value of the <code>MY_LIB</code> variable will be set to the full path of the library.</p><p>You can use the <code>find_library</code> command to locate library files on the system. You need to provide a variable name (to store the path of the found library) and the name of the library.</p><p>For example, suppose you want to locate a library named <code>libexample</code> that is in the <code>/usr/local/lib</code> directory. The absolute path can be converted to a relative path using variables such as <code>${CMAKE_CURRENT_LIST_DIR}</code>. You can add the following command to your <code>CMakeLists.txt</code> file:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>find_library(EXAMPLE_LIB libexample /usr/local/lib)</span></span></code></pre></div><p>This command will store the path of the <code>libexample</code> library in the <code>EXAMPLE_LIB</code> variable. If the library cannot be found, the value of the <code>EXAMPLE_LIB</code> variable will be empty.</p><p>When using the <code>find_library</code> command, you can specify the name, path, version, and language of the library. For example, the command <code>find_library(EXAMPLE_LIB NAMES example PATHS /usr/local/lib VERSION 1.0 LANGUAGES CXX)</code> will locate a library named <code>example</code> with version <code>1.0</code> and written in the <code>C++</code> language, and store its path in the <code>EXAMPLE_LIB</code> variable.</p><p>If you want to locate multiple libraries, you can add multiple library names to the command. For example, the command <code>find_library(LIB1 NAMES lib1 lib1.a PATHS /usr/local/lib)</code> will locate a library named <code>lib1</code> or <code>lib1.a</code>, and store its path in the <code>LIB1</code> variable.</p><p>Note that when using the <code>find_library</code> command, you need to ensure that the name, path, version, and language of the library match your project. Otherwise, your project may not link to the library file correctly.</p><h3 id="target-link-libraries" tabindex="-1"><code>target_link_libraries</code> <a class="header-anchor" href="#target-link-libraries" aria-label="Permalink to &quot;`target_link_libraries`&quot;">​</a></h3><p>The <code>target_link_libraries()</code> command is used to link specified libraries to a target. This command requires at least two arguments: the target name and the library name. For example, <code>target_link_libraries(MyApp MyLib)</code> will link the <code>MyLib</code> library to the <code>MyApp</code> target. This means that <code>MyApp</code> will use <code>MyLib</code> during the build process.</p><h3 id="target-include-directories" tabindex="-1"><code>target_include_directories</code> <a class="header-anchor" href="#target-include-directories" aria-label="Permalink to &quot;`target_include_directories`&quot;">​</a></h3><p>The <code>target_include_directories()</code> command is used to add include directories to a specified target. This command requires at least two arguments: the target name and the directory to be added. For example, <code>target_include_directories(MyApp PRIVATE include/)</code> will add the <code>include/</code> directory to the include directories of the <code>MyApp</code> target. This means that during compilation of <code>MyApp</code>, the compiler will search for header files in the <code>include/</code> directory.</p><h3 id="target-compile-options" tabindex="-1"><code>target_compile_options</code> <a class="header-anchor" href="#target-compile-options" aria-label="Permalink to &quot;`target_compile_options`&quot;">​</a></h3><p>The <code>target_compile_options()</code> command is used to set compilation options for a specified target. This command requires at least two arguments: the target name and the compilation option. For example, <code>target_compile_options(MyApp PRIVATE -Wall)</code> will add the <code>-Wall</code> option to the compilation options of <code>MyApp</code>. This means that <code>MyApp</code> will enable all warnings during compilation (which is the effect of the <code>-Wall</code> option).</p><h2 id="common-tasks" tabindex="-1">Common Tasks <a class="header-anchor" href="#common-tasks" aria-label="Permalink to &quot;Common Tasks&quot;">​</a></h2><h3 id="adding-source-files" tabindex="-1">Adding Source Files <a class="header-anchor" href="#adding-source-files" aria-label="Permalink to &quot;Adding Source Files&quot;">​</a></h3><p>You can use the following command to add source files:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>add_executable(MyProject main.cpp math/vec3.cpp math/vec4.cpp)</span></span></code></pre></div><p>In this example, the source files for <code>MyProject</code> include <strong><code>main.cpp</code></strong>, <code>math/vec3.cpp</code>, and <code>math/vec4.cpp</code>. If there are more source files, simply add them to this list.</p><h3 id="adding-dependency-libraries" tabindex="-1">Adding Dependency Libraries <a class="header-anchor" href="#adding-dependency-libraries" aria-label="Permalink to &quot;Adding Dependency Libraries&quot;">​</a></h3><p>You can use the following command to add dependency libraries:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>target_link_libraries(MyProject MyLibrary)</span></span></code></pre></div><p>In the example below, the <code>find_library()</code> command searches for a static library named <code>libexample</code> in the <code>libs</code> directory and saves its path to the <code>LIBS</code> variable. The <code>target_link_libraries()</code> command links this library to the <code>MyProject</code> target.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>find_library(LIBS libexample libs PATHS ${CMAKE_CURRENT_LIST_DIR}/libs/android/${ANDROID_ABI})</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span>add_executable(MyProject main.cpp)</span></span>\n<span class="line"><span>target_link_libraries(MyProject ${LIBS})</span></span>\n<span class="line"><span></span></span>\n<span class="line"><span># add include search path</span></span>\n<span class="line"><span>target_include_directories(MyProject  PUBLIC ${CMAKE_CURRENT_LIST_DIR}/libs/include)</span></span></code></pre></div><h3 id="cmake-variables" tabindex="-1">CMake Variables <a class="header-anchor" href="#cmake-variables" aria-label="Permalink to &quot;CMake Variables&quot;">​</a></h3><p>CMake has some built-in variables starting with <code>CMAKE_</code> that are useful for interacting with the environment. Using these variables can make your CMakeLists.txt file more concise and easier to maintain. For example, the <code>CMAKE_CURRENT_LIST_DIR</code> variable is used to store the path of the directory where the currently processed CMakeLists.txt file is located. An example of using this variable in a CMakeLists.txt file is as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>add_library(MyLibrary STATIC ${CMAKE_CURRENT_LIST_DIR}/src/my_library.cpp)</span></span></code></pre></div><p>In the above example, we use the <code>CMAKE_CURRENT_LIST_DIR</code> variable to specify the path of the source file. Similarly, the <code>CMAKE_BINARY_DIR</code> variable is used to store the path of the root directory of the binary files. An example of using this variable in a CMakeLists.txt file is as follows:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>set(EXECUTABLE_OUTPUT_PATH ${CMAKE_BINARY_DIR}/bin)</span></span></code></pre></div><p>Here, the CMAKE_BINARY_DIR variable is used to specify the root directory for executable file output. When compiling the project, the executable file will be output to the ${CMAKE_BINARY_DIR}/bin directory.</p><p>Note the difference between the <code>${CMAKE_BINARY_DIR}</code> and <code>${CMAKE_CURRENT_BINARY_DIR}</code> variables. <code>${CMAKE_BINARY_DIR}</code> refers to the root directory of the binary files, while <code>${CMAKE_CURRENT_BINARY_DIR}</code> refers to the binary directory of the currently processed CMakeLists.txt file.</p><p>Additionally, other commonly used variables include but are not limited to:</p><ul><li>CMAKE_SOURCE_DIR: The directory where the CMakeLists.txt file is located</li><li>CMAKE_CURRENT_SOURCE_DIR: The directory where the currently processed CMakeLists.txt file is located</li><li>CMAKE_BINARY_DIR: The root directory of the binary files</li><li>CMAKE_CURRENT_BINARY_DIR: The binary directory of the currently processed CMakeLists.txt file</li><li>CMAKE_INSTALL_PREFIX: The root directory of the installation path</li><li>CMAKE_MODULE_PATH: The root directory of the CMake modules</li><li>CMAKE_BUILD_TYPE: The build type</li><li>CMAKE_CXX_FLAGS: C++ compiler options</li></ul><h2 id="using-cmake-in-cocos" tabindex="-1">Using CMake in Cocos <a class="header-anchor" href="#using-cmake-in-cocos" aria-label="Permalink to &quot;Using CMake in Cocos&quot;">​</a></h2><p>CMake is natively supported in Android for compiling C++ code. We use Gradle to configure parameters and call CMake commands to generate, compile, and package C++ code. For other native platforms, we use build plugins to call the corresponding CMake commands to generate project files, such as Visual Studio projects on Windows and Xcode projects on Mac. Subsequent development can be completed through the IDE.</p><p>Due to the nature of CMake, there may be differences due to different development environments and configurations, so it is not recommended to share generated project files. In addition, modifications to the generated project can be easily overwritten by subsequent generated projects. Instead, the CMakeLists.txt file should be included in the project, and CMake should be used in each development environment to generate the corresponding project files. All modifications to the project should be written in CMake instructions in CMakeLists.txt.</p><p>When using CMake and Xcode cocoapods, some issues may arise. The main issue is that the Xcode project files generated by CMake are incompatible with cocoapods integration. This is because cocoapods uses its way of managing Xcode project files, and the generated project files by CMake do not consider this. This can cause problems such as compilation errors, link errors, and overwritten modifications.</p><p>To solve this problem, we can select the &quot;Skip Xcode Project Update&quot; option when building for Mac/iOS platforms. This means that subsequent CMake configuration updates for the engine or project will not be synchronized to the Xcode project. After selecting this option, we can collaborate with CocoaPods and build in a regular Xcode project format.</p><h3 id="share-generated-xcode-project-files" tabindex="-1">Share Generated Xcode Project Files <a class="header-anchor" href="#share-generated-xcode-project-files" aria-label="Permalink to &quot;Share Generated Xcode Project Files&quot;">​</a></h3><p>The Xcode project generated by CMake records dependency paths from the following sources:</p><ul><li>The installation path of Xcode</li><li>The version and installation path of Cocos Creator</li><li>The version and installation path of the project</li><li>The path where the project file is located</li></ul><p>By using the same directory structure on different devices, Xcode can achieve sharing between different devices.</p><p>Another approach is to modify the paths referenced internally in the Xcode project file, which is a more hacky method and will not be discussed in detail here.</p><p>In contrast to Xcode, Android Studio compiles CMakeLists.txt directly as the configuration file instead of generating project files. Therefore, the native library generated by CMake for the Android platform should behave consistently on different devices. In addition, the Gradle plugin of Android Studio automatically handles dependency relationships, so there is no need to manually manage dependency directories like in Xcode.</p><h3 id="directory-structure" tabindex="-1">Directory structure <a class="header-anchor" href="#directory-structure" aria-label="Permalink to &quot;Directory structure&quot;">​</a></h3><p>When selecting a native platform to build on, the <code>native\\engine</code> directory will generate a <code>current build platform name</code> folder (e.g.: <code>android</code>) and a <code>common</code> folder, and CMake will generate <code>CMakeLists.txt</code> files in each of these two directories when it is first run, with which serve different purposes:</p><ul><li><p>In the <code>current build platform name</code> folder: <code>CMakeLists.txt</code> is mainly used to configure the corresponding build platform. For the Android platform, for example:</p><p><img src="'+t+'" alt="folder2"></p></li><li><p>In the <code>common</code> folder: <code>CMakeLists.txt</code> is mainly used to configure the whole project.</p><p><img src="'+o+`" alt="folder2"></p></li></ul><p>The syntax of <code>CMakeLists.txt</code> is relatively simple, consisting of <strong>commands</strong>, <strong>comments</strong> and <strong>spaces</strong>. The commands are case-insensitive, but the parameters and variables in the commands are case-sensitive.</p><h3 id="cmakelists-txt-comments" tabindex="-1">CMakeLists.txt comments <a class="header-anchor" href="#cmakelists-txt-comments" aria-label="Permalink to &quot;CMakeLists.txt comments&quot;">​</a></h3><p>How to use CMake to compile a project into a dynamic library for other projects? Simply put, enter the compilation information first, and then the CMake command generates the Makefile file needed for compilation according to the configuration in <code>CMakeLists.txt</code>.</p><p>Taking the Android platform as an example and see how to configure <code>CMakeLists.txt</code> in the project directory <code>native/engine/android</code>.</p><div class="language-CMake vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Require CMake version 3.8 or higher </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">cmake_minimum_required</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">VERSION</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> 3.8)  </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Set project name option</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">option</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(APP_NAME </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Project name&quot;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;NewProject&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Set project name and enable C++</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">project</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${APP_NAME}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> CXX)  </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Set library name  </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CC_LIB_NAME cocos)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Set project directory</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CC_PROJECT_DIR </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${CMAKE_CURRENT_LIST_DIR}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)  </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Set project source files </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CC_PROJ_SOURCES)  </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Set common source files</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CC_COMMON_SOURCES)</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Set all source files </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">set</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(CC_ALL_SOURCES)  </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Include common CMake functions</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">include</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${CC_PROJECT_DIR}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/../common/CMakeLists.txt) </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># If you need to add source code, you can modify CC_PROJ_SOURCES here </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Call Android pre-build steps</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cc_android_before_target(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${CC_LIB_NAME}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)  </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Add library </span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">add_library</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${CC_LIB_NAME}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> SHARED </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${CC_ALL_SOURCES}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)  </span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Call Android post-build steps</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">cc_android_after_target(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${CC_LIB_NAME}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The <code>CMakeLists.txt</code> file in the project directory <code>native/engine/common</code> is configured in the same way but with some more basic configuration. For example:</p><div class="language-CMake vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">option</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(USE_SPINE                </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Enable Spine&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">                      ON</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span></code></pre></div><p>The release package directory generated after the build (e.g.: <code>build/android</code>) has a <code>proj/cfg.cmake</code> file to store some configuration for the current project. Since <code>CMakeLists.txt</code> introduces the <code>cfg.cmake</code> file, when the configuration in the <code>cfg.cmake</code> file is modified, it will be synchronized to <code>CMakeLists.txt</code>; if the configuration is the same, it will be overwritten directly, and the one in the <code>cfg.cmake</code> file will prevail.</p><p>Starting from 3.6.2, developers can override the options written in <code>cfg.cmake</code> through <code>native/engine/common/localCfg.cmake</code>, and the file <code>localCfg.cmake</code> will be ignored from the GIT.</p><div class="language-CMake vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">CMake</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">CMakeLists.txt</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Introduce cfg.cmake</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">include</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">\${RES_DIR}</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">/proj/cfg.CMake)</span></span></code></pre></div><p>For example, uncheck <strong>Spine Animation</strong> in the editor&#39;s main menu <strong>Project -&gt; Project Settings -&gt; Feature Cropping</strong>.</p><p><img src="`+n+'" alt="project"></p><p>Then <code>USE_SPINE</code> will be set to <code>OFF</code> in the re-generated <code>cfg.make</code> when building again:</p><p><img src="'+l+'" alt="code1"></p><p>Then at compile time, CMake generates a <strong>CMakeCache.txt</strong> file based on the configuration (e.g.: <code>CMakeLists.txt</code> and the <code>cfg.make</code> configuration file introduced in <code>CMakeLists.txt</code>), which contains the <strong>various input parameters that the project needs to rely on</strong> when building.</p><p><img src="'+c+'" alt="code2"></p><h3 id="further-learning" tabindex="-1"><strong>Further Learning</strong> <a class="header-anchor" href="#further-learning" aria-label="Permalink to &quot;**Further Learning**&quot;">​</a></h3><p>You can refer to the following resources for further learning: <a href="https://cmake.org/documentation/" target="_blank" rel="noreferrer">CMake Official Documentation</a></p>',116),p=[d];function h(u,m,g,k,b,y){return s(),a("div",null,p)}const E=e(r,[["render",h]]);export{C as __pageData,E as default};
